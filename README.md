# Presentation Webpage

This is a project I created as a curriculum/presentation site. At first I used only HTML and CSS, but I quickly realized I could use Jekyll to make it easier to manage and update the content.

## How to run
Development environmnet
```zsh
bundle exec jekyll serve

# if Just is available
just serve
```